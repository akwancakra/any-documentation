services:
  postgres:
    image: postgres:16-alpine
    container_name: any-documentation-db
    restart: unless-stopped
    profiles:
      - with-postgres
    environment:
      POSTGRES_USER: wiki
      POSTGRES_PASSWORD: wiki
      POSTGRES_DB: wiki
    ports:
      - "5432:5432"
    volumes:
      - wiki_pg_data:/var/lib/postgresql/data
    networks:
      - network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wiki -d wiki"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: any-documentation
    restart: unless-stopped
    user: "1001:1001" # Match container user
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
      # OLLAMA_API_KEY=your-api-key
      # OLLAMA_AI_MODEL=model-name
      # GEMINI_API_KEY=your-api-key
      # GEMINI_AI_MODEL=model-name
      # Tambahkan environment variables lain sesuai kebutuhan
      # - NEXTAUTH_URL=https://yourdomain.com
      # - NEXTAUTH_SECRET=your-secret-key
    volumes:
      # Mount content directory untuk edit file langsung di server
      - ./content:/app/content:rw
      # Optional: Mount public files jika perlu edit
      - ./public:/app/public:rw
    networks:
      - network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Optional: Nginx reverse proxy untuk production
  nginx:
    image: nginx:alpine
    container_name: any-documentation-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      # SSL certificates jika ada
      # - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - app
    networks:
      - network
    profiles:
      - with-nginx

volumes:
  wiki_pg_data:

networks:
  network:
    driver: bridge
# Optional: Untuk development/backup
# volumes:
#   content_data:
#   public_data:
